import { Router } from "express";

const router = Router();

router.post("/", () => {});
router.get("/", () => {});
router.patch("/:vendor_id", () => {});
router.delete("/:vendor_id", () => {});

export default router;
